/* Tags */

a {
  color: $blue;
}

h1 {
  font-size: 22px;
  text-transform: uppercase;
  font-family: 'BenchNine', sans-serif;
  color: $blue;
}

h2 {
  font-size: 36px;
  text-transform: uppercase;
  font-family: 'BenchNine', sans-serif;
  color: $blue;
}

h3 {
  font-size: 20px;
  text-transform: uppercase;
  font-family: 'BenchNine', sans-serif;
  color: $blue;
}

ul {
  list-style-position: outside;

  li {
    background-image: url(<%= asset_path('indicator.png') %>);
    background-position: 0px 4px;
    background-repeat: no-repeat;
    line-height: 25px;
    padding-left: 25px;
  }
}

img {
  padding: 10px;
  background-color: $gray;
}

p {
  line-height: 22px;
}

strong {
  font-weight: bold;
}

em {
  font-style: italic;
}

hr {
  border: 0px;
  outline: 0px;
  height: 1px;
  background-color: $gray;
  margin-bottom: 20px;
}

form {
  div {
    margin-bottom: 10px;
  }

  input[type=text], input[type=email], input[type=tel], textarea {
    width: 545px;
    border: none;
    outline: none;
    background-color: $gray;
    padding: 10px;
    color: $blue;
  }

  input[type=submit] {
    float: right;
    border: none;
    outline: none;
    padding: 10px 16px;
    margin: 0px;
    background-color: $blue;
    color: $white;
    font-size: 16px;
    text-transform: uppercase;
    font-family: 'BenchNine', sans-serif;
    cursor: pointer;
  }

  input[type=submit]:hover {
    background-color: $gray;
    color: $blue;
  }
}
